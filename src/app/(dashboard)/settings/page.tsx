import Settings from '@/components/Settings/Settings';
import React from 'react';

const Page = () => {
    return (
        <div>
            <Settings />
        </div>
    );
};

export default Page;